import React from 'react';
import { Layout} from 'antd';

const { Footer } = Layout;

const FooterBar: React.FC = () => {
    return (
        <Footer style={{ textAlign: 'center' }}>Noon ©2023 Created by Noon UED</Footer>
  );
};

export default FooterBar;